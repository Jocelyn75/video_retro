{% extends 'base.html.twig' %}

{% block title %}DetailsCommandes{% endblock %}

{% block body %}
    <h1>DetailsCommandes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ details_commande.id }}</td>
            </tr>
            <tr>
                <th>Commandes_id</th>
                <td>{{ details_commande.commandesId }}</td>
            </tr>
            <tr>
                <th>Stock_id</th>
                <td>{{ details_commande.stockId }}</td>
            </tr>
            <tr>
                <th>Quantite_cmd</th>
                <td>{{ details_commande.quantiteCmd }}</td>
            </tr>
            <tr>
                <th>Prix_unitaire (€)</th>
                <td>{{ details_commande.prixUnitaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_details_commandes_index') }}">back to list</a>

    <a href="{{ path('app_details_commandes_edit', {'id': details_commande.id}) }}">edit</a>

    {{ include('details_commandes/_delete_form.html.twig') }}
{% endblock %}
