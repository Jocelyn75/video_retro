<form method="post" action="{{ path('app_details_commandes_delete', {'id': details_commande.id}) }}" onsubmit="return confirm('Confirmer la suppression ? ');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ details_commande.id) }}">
    <button class="btn">Delete</button>
</form>
