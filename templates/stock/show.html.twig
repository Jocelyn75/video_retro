{% extends 'base.html.twig' %}

{% block title %}<title>Stock</title>{% endblock %}

{% block body %}
    {% include '_header.html.twig' %}

    {# Fil d'Ariane #}
    <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item ms-3"><a class="aBreadCrumb" href="{{path('home')}}">Accueil</a></li>
            <li class="breadcrumb-item"><a class="aBreadCrumb" href="{{path('app_admin_home')}}">Back office</a></li>
            <li class="breadcrumb-item"><a class="aBreadCrumb" href="{{path('app_stock_index')}}">Liste des films</a></li>
            <li class="breadcrumb-item active" aria-current="page">{{ filmTitle }}</li>
        </ol>
    </nav>

    <h1>Stock</h1>
    <main class="container">{% block main %}
        <h2>{{ filmTitle }}</h2>
            <table class="table">
                <tbody>
                        <th>Id Produit</th>
                        <td>{{ stock.id }}</td>
                    </tr>
                    <tr>
                        <th>Format</th>
                        <td>{{ stock.formats.nomFormat }}</td>
                    </tr>
                    <tr>
                        <th>Prix de revente</th>
                        <td>{{ stock.prixReventeDefaut }}</td>
                    </tr>
                    <tr>
                        <th>Quantité en stock</th>
                        <td>{{ stock.quantiteStock }}</td>
                    </tr>
                    <tr>
                        <th>Id du film</th>
                        <td>{{ stock.filmsId }}</td>
                    </tr>

                    <tr>
                        <th>Film API Id</th>
                        <td>{{ stock.films.filmsApiId }}</td>
                    </tr>

                </tbody>
            </table>

        <a href="{{ path('app_stock_index') }}">back to list</a>

        <a href="{{ path('app_stock_edit', {'id': stock.id}) }}">edit</a>

        {# {{ include('stock/_delete_form.html.twig') }} #}
    {% endblock %}</main>
    {% include "_footer.html.twig" %}
{% endblock %}
