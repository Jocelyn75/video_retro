<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Accueil{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        <link rel="stylesheet" href="{{ asset('css/style.css')}}">
        {% block stylesheets %}{% endblock %}
        <script src="https://kit.fontawesome.com/428d5badb7.js" crossorigin="anonymous"></script>
    </head>


    <body>
        <header>
            {# Navbar 1 pour contenir le logo, la baseline et la barre de recherche #}
            <nav class="navbar navbar-expand-lg bg-secondary">
                <div class="container-fluid">
                    <a class="font-lazer" class="navbar-brand text-white" href="{{ path('home')}}">Video Retro</a>
                    <p class="slogan text-light align-self-items-center">Tout le cinéma en VHS, DVD et Blu-Ray<p>  
                    {# On rend la méthode GetSearchBar du controller SearchController. Les :: permettent d'appeler getSearchBar de manière statique. Les \\ sont une convention, cela ne fonctionne pas avec un seul \ #}
                    {{ render(controller("App\\Controller\\SearchController::getSearchBar")) }}              
                </div>
            </nav>

            {# NAVBAR 2 pour contenir les onglets Accueil, Inscription, Connexion, Déconnexion et produits qui sera à rajouter. #}
            <nav class="navbar navbar-expand-lg bg-secondary">
                {# Mise en place du bouton de navigation qui remplace le menu pour les écrans tablettes et mobiles. #}
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        {# Si tel nom de route est celui de la route actuelle, alors on applique la classe ongletActif pour mettre cet onglet en évidence  #}
                        <li class="nav-item {% if app.request.attributes.get('_route') == 'home' %} ongletActif {% endif %}">
                            <a class="nav-link text-white " href="{{ path('home') }}">Accueil</a>
                        </li>
                        {# Si l'utilisateur est connecté, seul le bouton Déconnexion s'affiche #}
                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link text-white" href="{{ path('app_logout') }}">Déconnexion</a>
                            </li>
                            
                        {# S'il n'y a pas d'utilisateur connecté, les boutons Inscription et Connexion s'affichent #}
                        {% else %}
                            
                            <li class="nav-item {% if app.request.attributes.get('_route') == 'app_register' %} ongletActif {% endif %}">
                                <a class="nav-link text-white " href="{{ path('app_register') }}">Inscription</a>
                            </li>
                            <li class="nav-item {% if app.request.attributes.get('_route') == 'app_login' %} ongletActif {% endif %}">
                                <a class="nav-link text-white" href="{{ path('app_login') }}">Connexion</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </nav>
        </header>

        {# MAIN #}
        <main class="container">
            <h1 class="text-center text-secondary">{% block h1 %}{% endblock %}</h1>
        {% block body %}{% endblock %}
        </main>

        {# FOOTER #}
        <footer class="footer bg-secondary mt-3">
            <div class="container">
                <div class="row">
                    {# Section contact #}
                    <section class="col-md-6">
                        <h5 class="text-white fw-bold">Contact</h5>
                        <p class="text-white mb-1"><i class="fas fa-square-envelope me-1"></i>contact@videoretro.fr</p>
                        <p class="text-white mb-1"><i class="fas fa-square-phone me-1"></i>+336 89 72 94 54</p>
                        <p class="text-white mb-1"><i class="fa-solid fa-location-dot me-1"></i>9 rue de La Rochette - 77000 Melun - FRANCE</p>
                    </section>

                    {# Section mentions légales #}
                    <section class="col-md-6">
                    {# offset permet de décaler le contenu (ici vers la droite) #}
                    <div class="offset-md-6">
                        <h5 class="text-white fw-bold">A propos</h5>
                        <p class="text-white mb-1"><i class="fa-solid fa-circle-info me-1"></i>Mentions légales</p>
                        <p class="text-white mb-1"><i class="fa-solid fa-circle-info me-1"></i>Conditions générales de vente</p>
                        <p class="text-white mb-1"><i class="fa-solid fa-circle-info me-1"></i>Conditions générales d'utilisation</p>
                        </div>
                    </section>
                </div>
            </div>

            {# Section copyright #}
            <section>
                <p class="text-white mb-1 text-center"><i class="fa-regular fa-copyright"></i> 2024 Copyright - Jocelyn Dufour</p>
            </section>

        </footer>


        {# <footer class="footer bg-secondary fixed-bottom">
        <div>
            <p>Nous contacter</p>
            Ajouter ci-dessous le lien actif vers le mail de contact
            <p>contact@videoretro.fr</p>
            <p>Tél : +33612345678</p>
        </div>
        </footer> #}




    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    {% block javascript %}{% endblock %}
    </body>

</html>