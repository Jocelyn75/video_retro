<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Accueil{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        <link rel="stylesheet" href="{{ asset('css/style.css')}}">
        {% block stylesheets %}{% endblock %}
    </head>


    <body>

        <nav class="navbar navbar-expand-lg bg-secondary">

            <div class="container-fluid">
                <a class="logo" class="navbar-brand text-white" href="{{ path('home')}}">Video Retro</a>
                <p class="slogan text-light align-self-items-center">Tout le cinéma en VHS, DVD et Blu-Ray<p>  
                {{ render(controller("App\\Controller\\SearchController::getSearchBar")) }}              
                {# <form class="d-flex" role="search">
                <input class="form-control me-auto" type="search" placeholder="Rechercher" aria-label="Search">
                <button class="btn btn-outline-light me-auto" type="submit">Rechercher</button>
            </form> #}
            </div>
        </nav>

        <nav class="navbar navbar-expand-lg bg-secondary">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                    {# Si l'utilisateur est connecté, seul le bouton Déconnexion s'affiche #}
                    {% if app.user %}
                        <li class="nav-item">
                            <a class="nav-link text-white" href="{{ path('app_logout') }}">Déconnexion</a>
                        </li>
                        
                    {# S'il n'y a pas d'utilisateur connecté, les boutons Inscription et Connexion s'affichent #}
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link text-white" href="{{ path('app_register') }}">Inscription</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-white" href="{{ path('app_login') }}">Connexion</a>
                        </li>
                    {% endif %}
                </ul>
                </div>
        </nav>
    
        <h1 class="text-center text-secondary">{% block h1 %}{% endblock %}</h1>

        <div class="container">
        {% block body %}{% endblock %}
        </div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    {% block javascript %}{% endblock %}
    </body>

</html>