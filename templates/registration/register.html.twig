{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    {{ form_errors(registrationForm) }}


    {# Comment modifier l'espacement entre le label et son champ de formulaire ? #}
    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email,{'label_attr': {'class': 'mb-0'}})}}
        {{ form_row(registrationForm.plainPassword,{'label_attr': {'class': 'mb-0'}})}}
        {# {{ form_row(registrationForm.nom) }}
        {{ form_row(registrationForm.prenom,{
            label: 'Prénom'
        }) }}
        {{ form_row(registrationForm.date_naiss, {
            label: 'Date de naissance'
        }) }}

        {{ form_row(registrationForm.adr_user, {
            label: 'Adresse'
        }) }}

        {{ form_row(registrationForm.complement_adr, {
            label: 'Complément d\'adresse'
        }) }}

        {{ form_row(registrationForm.code_postal, {
            label: 'Code postal'
        }) }}

        {{ form_row(registrationForm.ville, {
            label: 'Ville'
        }) }}

        {{ form_row(registrationForm.tel_user, {
            label: 'Numéro de téléphone'
        }) }} #}

        {{ form_row(registrationForm.agreeTerms, {
            label : 'Accepter les termes et conditions d\'utilisation'
        }) }}

        <button type="submit" class="btn btn-secondary w-100">S'inscrire</button>
    {{ form_end(registrationForm) }}
{% endblock %}


