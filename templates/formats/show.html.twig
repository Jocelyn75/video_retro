{% extends 'base.html.twig' %}

{% block title %}Formats{% endblock %}

{% block body %}
    <h1>Formats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ format.id }}</td>
            </tr>
            <tr>
                <th>Nom_format</th>
                <td>{{ format.nomFormat }}</td>
            </tr>
            <tr>
                <th>Prix_rachat_defaut</th>
                <td>{{ format.prixRachatDefaut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formats_index') }}">back to list</a>

    <a href="{{ path('app_formats_edit', {'id': format.id}) }}">edit</a>

    {{ include('formats/_delete_form.html.twig') }}
{% endblock %}
