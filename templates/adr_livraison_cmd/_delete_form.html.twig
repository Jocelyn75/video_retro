<form method="post" action="{{ path('app_adr_livraison_cmd_delete', {'id': adr_livraison_cmd.id}) }}" onsubmit="return confirm('Confirmer la suppression ? ');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adr_livraison_cmd.id) }}">
    <button class="btn">Delete</button>
</form>
