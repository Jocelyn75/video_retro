{% extends 'base.html.twig' %}

{% block title %}AdrLivraisonCmd{% endblock %}

{% block body %}
    <h1>AdrLivraisonCmd</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adr_livraison_cmd.id }}</td>
            </tr>
            <tr>
                <th>Adr_livr_cmd</th>
                <td>{{ adr_livraison_cmd.adrLivrCmd }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adr_livraison_cmd_index') }}">back to list</a>

    <a href="{{ path('app_adr_livraison_cmd_edit', {'id': adr_livraison_cmd.id}) }}">edit</a>

    {{ include('adr_livraison_cmd/_delete_form.html.twig') }}
{% endblock %}
