{% extends 'base.html.twig' %}

{% block title %}AdrLivraisonUser{% endblock %}

{% block body %}
    <h1>AdrLivraisonUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adr_livraison_user.id }}</td>
            </tr>
            <tr>
                <th>Adr_livr_user</th>
                <td>{{ adr_livraison_user.adrLivrUser }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adr_livraison_user_index') }}">back to list</a>

    <a href="{{ path('app_adr_livraison_user_edit', {'id': adr_livraison_user.id}) }}">edit</a>

    {{ include('adr_livraison_user/_delete_form.html.twig') }}
{% endblock %}
