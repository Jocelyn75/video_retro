<form method="post" action="{{ path('app_adr_livraison_user_delete', {'id': adr_livraison_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adr_livraison_user.id) }}">
    <button class="btn">Delete</button>
</form>
